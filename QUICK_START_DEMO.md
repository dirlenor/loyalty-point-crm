# 🚀 Quick Start: ทดสอบระบบ Demo Topup

## ⚡ เริ่มต้นใน 5 นาที

### ขั้นตอนที่ 1: ตั้งค่า Environment Variables (2 นาที)

1. เปิดไฟล์ `.env.local` ในโฟลเดอร์โปรเจค
2. เพิ่มบรรทัดเหล่านี้ (ถ้ายังไม่มี):

```env
DEMO_MODE=true
DEMO_ENABLED=true
PROMPTPAY_API_KEY=test_demo_key_12345
PROMPTPAY_API_SECRET=test_demo_secret_67890
PROMPTPAY_SANDBOX_URL=https://sandbox-api.promptpay.com
PROMPTPAY_WEBHOOK_SECRET=test_webhook_secret_abc123
DEMO_POINT_RATE=1
DEMO_QR_EXPIRY_MINUTES=15
```

3. บันทึกไฟล์

### ขั้นตอนที่ 2: Restart Server (30 วินาที)

```bash
# กด Ctrl+C เพื่อหยุด server (ถ้ากำลังรันอยู่)
# จากนั้นรันใหม่
npm run dev
```

### ขั้นตอนที่ 3: ทดสอบสร้าง Topup Order (1 นาที)

1. เปิดเบราว์เซอร์ไปที่: `http://localhost:3000/admin/demo-topup`
2. กรอกข้อมูล:
   - **User ID**: คัดลอก Profile ID จาก `/admin/customers` (เลือกลูกค้าใดก็ได้)
   - **จำนวนเงิน**: `100`
3. กด **สร้าง QR Code**
4. **คัดลอก Order ID และ Transaction ID** (จะใช้ในขั้นตอนถัดไป)

### ขั้นตอนที่ 4: ทดสอบ Webhook (1 นาที)

1. ไปที่: `http://localhost:3000/admin/demo-topup/test-webhook`
2. วาง Order ID และ Transaction ID ที่คัดลอกไว้
3. ตรวจสอบจำนวนเงิน (ควรเป็น 100)
4. กด **ส่ง Webhook**

### ขั้นตอนที่ 5: ตรวจสอบผลลัพธ์ (30 วินาที)

1. ไปที่: `http://localhost:3000/admin/demo-topup/orders`
2. ดู Order ที่สร้างไว้ - สถานะควรเป็น **"สำเร็จ"** ✅
3. ไปที่: `http://localhost:3000/customer/demo-wallet`
4. ตรวจสอบว่าแต้มถูกเพิ่มแล้ว (100 แต้ม) ✅

---

## 🎉 เสร็จแล้ว!

ระบบทำงานได้แล้ว! ตอนนี้คุณสามารถ:

- ✅ สร้าง Topup Order ได้
- ✅ ส่ง Webhook เพื่อจำลองการชำระเงินได้
- ✅ ดูแต้มใน Demo Wallet ได้

---

## 📚 ต้องการข้อมูลเพิ่มเติม?

อ่านคู่มือฉบับเต็มได้ที่: `DEMO_TOPUP_SETUP.md`

---

## ❓ ปัญหาที่พบบ่อย

**Q: ไม่เห็นเมนู Demo Topup**
- ตรวจสอบว่า restart server แล้ว
- ตรวจสอบว่า `.env.local` มี `DEMO_MODE=true`

**Q: Webhook ไม่ทำงาน**
- ตรวจสอบว่า Order ID และ Transaction ID ถูกต้อง
- ดู Console Logs ใน Terminal

**Q: แต้มไม่ถูกเพิ่ม**
- ตรวจสอบว่า Webhook ส่งสำเร็จ (Status 200)
- ตรวจสอบ Order Status ว่าเป็น "success"

